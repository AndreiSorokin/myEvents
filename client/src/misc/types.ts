export type Address = {
   country: string;
   city: string;
   district: string;
   post_code: string;
}

export const InitialStateAddress = {
   country: '',
   city: '',
   district: '',
   post_code: '',
}